spring:
  application:
    name: KalingaLumiere-Backend

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: KalingaLumiereHikariPool
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        default_batch_fetch_size: 20
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: ${MULTIPART_MAX_FILE_SIZE:5MB}
      max-request-size: ${MULTIPART_MAX_REQUEST_SIZE:6MB}


  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    validate-on-migrate: true
    out-of-order: false

  jackson:
    time-zone: Asia/Kolkata
    serialization:
      write-dates-as-timestamps: false
      fail-on-empty-beans: false
    deserialization:
      fail-on-unknown-properties: false

server:
  port: ${SERVER_PORT}
  servlet:
    context-path: /
  compression:
    enabled: true
    mime-types: application/json,text/html,text/plain

app:
  storage:
    provider: ${STORAGE_PROVIDER:local}
    path: ${STORAGE_PATH:uploads}
    max-logo-bytes: ${MAX_LOGO_BYTES:2097152}
    employee-documents:
      profile-photo-max-size: ${EMPLOYEE_PROFILE_PHOTO_MAX_SIZE:200KB}
      pan-max-size: ${EMPLOYEE_PAN_MAX_SIZE:1MB}
      identity-proof-max-size: ${EMPLOYEE_IDENTITY_PROOF_MAX_SIZE:2MB}
      bank-proof-max-size: ${EMPLOYEE_BANK_PROOF_MAX_SIZE:5MB}
      certificate-max-size: ${EMPLOYEE_CERTIFICATE_MAX_SIZE:2MB}
      driving-license-max-size: ${EMPLOYEE_DRIVING_LICENSE_MAX_SIZE:1MB}
      appointment-letter-max-size: ${EMPLOYEE_APPOINTMENT_LETTER_MAX_SIZE:2MB}
      salary-slip-acknowledgement-max-size: ${EMPLOYEE_SALARY_SLIP_ACK_MAX_SIZE:2MB}
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION}
    refresh-expiration: ${JWT_REFRESH_EXPIRATION}
  cors:
    allowed-origins: ${CORS_ORIGINS}
  security:
    trust-proxy-headers: ${TRUST_PROXY_HEADERS:false}
  timezone: Asia/Kolkata
  mail:
    from: ${MAIL_FROM}
  frontend:
    reset-password-url: ${FRONTEND_RESET_PASSWORD_URL}

logging:
  level:
    com.business.erp: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
    org.flywaydb: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n"
